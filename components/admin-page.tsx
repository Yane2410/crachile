import { useMemo, useState } from "react";
import { Link } from "@tanstack/react-router";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { ArrowLeft, ImagePlus, LogOut, Plus, Trash2 } from "lucide-react";
import { toast } from "sonner";
import { BrandLockup } from "@/components/brand";
import { ComboAdmin } from "@/components/combo-admin";
import { Button } from "@/components/ui/button";
import { FieldLabel, TextArea, TextField } from "@/components/ui/field";
import { Switch } from "@/components/ui/switch";
import { adminChangePin, adminCreateProduct, adminDeleteProduct, adminLogin, adminLogout, adminMe, adminPatchIngredient, adminPatchProduct, adminSaveSettings, getCatalog } from "@/lib/cra/fns";
import { cn } from "@/lib/cra/cn";
import { formatClp, normalizeWhatsapp } from "@/lib/cra/sanitize";
import type { Catalog, Product } from "@/lib/cra/types";
async function compressImage(file: File, max = 400_000) { const bitmap = await createImageBitmap(file); const scale = Math.min(1, 900 / Math.max(bitmap.width, bitmap.height)); const width = Math.max(1, Math.round(bitmap.width * scale)); const height = Math.max(1, Math.round(bitmap.height * scale)); const canvas = document.createElement("canvas"); canvas.width = width; canvas.height = height; const ctx = canvas.getContext("2d"); if (!ctx) throw new Error("No se pudo procesar la imagen."); ctx.drawImage(bitmap, 0, 0, width, height); let quality = 0.78; let data = canvas.toDataURL("image/jpeg", quality); while (data.length > max && quality > 0.35) { quality -= 0.08; data = canvas.toDataURL("image/jpeg", quality); } if (data.length > max) throw new Error("La imagen sigue siendo muy pesada. Prueba otra más liviana."); return data; }
function Login() { const queryClient = useQueryClient(); const [pin,setPin]=useState(""); const mutation=useMutation({mutationFn:()=>adminLogin({data:{pin}}),onSuccess:async()=>{await queryClient.invalidateQueries({queryKey:["admin-me"]});},onError:(error:Error)=>toast.error(error.message)}); return <div className="mx-auto flex min-h-dvh max-w-sm flex-col justify-center px-4"><BrandLockup/><h1 className="mt-8 font-display text-3xl font-semibold">Panel CRA</h1><p className="mt-2 text-sm text-muted">Entra con el PIN de la cocina para subir fotos, escribir descripciones y cambiar precios.</p><form className="mt-6 space-y-3" onSubmit={e=>{e.preventDefault();mutation.mutate()}}><FieldLabel htmlFor="pin">PIN</FieldLabel><TextField id="pin" type="password" inputMode="numeric" value={pin} onChange={e=>setPin(e.target.value)} autoFocus/><Button className="w-full" type="submit" disabled={mutation.isPending}>Entrar</Button></form><Link to="/" className="mt-6 text-center text-sm text-muted">Volver al menú</Link></div>; }
function PhotoPicker({imageUrl,uploading,onFile}:{imageUrl?:string;uploading:boolean;onFile:(file?:File)=>void}){return <label className="relative size-28 shrink-0 cursor-pointer overflow-hidden rounded-[var(--radius-md)] bg-surface-2">{imageUrl?<img src={imageUrl} alt="" className="size-full object-cover"/>:<span className="flex size-full flex-col items-center justify-center gap-1 text-muted"><ImagePlus className="size-6"/><span className="text-[10px] font-bold uppercase tracking-wide">Foto</span></span>}<span className="absolute inset-x-0 bottom-0 bg-fg/60 py-1 text-center text-[10px] font-bold text-primary-foreground">{uploading?"Subiendo…":imageUrl?"Cambiar foto":"Subir foto"}</span><input type="file" accept="image/*" className="absolute inset-0 z-10 cursor-pointer opacity-0" disabled={uploading} onChange={e=>onFile(e.target.files?.[0])}/></label>}
function NewProduct({onCreate,pending}:{onCreate:(data:{categoryId:string;name:string;price:number;description:string;imageUrl?:string})=>void;pending:boolean}){const[open,setOpen]=useState(false),[name,setName]=useState(""),[description,setDescription]=useState(""),[price,setPrice]=useState("3000"),[categoryId,setCategoryId]=useState("empanadas"),[imageUrl,setImageUrl]=useState(""),[uploading,setUploading]=useState(false);async function onFile(file?:File){if(!file)return;setUploading(true);try{setImageUrl(await compressImage(file))}catch(error){toast.error(error instanceof Error?error.message:"No se pudo subir")}finally{setUploading(false)}}if(!open)return <Button variant="secondary" className="w-full" onClick={()=>setOpen(true)}><Plus className="size-4"/>Nuevo producto</Button>;return <form className="space-y-3 rounded-[var(--radius-lg)] bg-surface p-4 shadow-[var(--shadow-border)]" onSubmit={e=>{e.preventDefault();onCreate({categoryId,name:name.trim(),price:Number(price)||0,description:description.trim(),imageUrl:imageUrl||undefined});setName("");setDescription("");setPrice("3000");setImageUrl("");setOpen(false)}}><p className="font-semibold">Nuevo producto</p><PhotoPicker imageUrl={imageUrl} uploading={uploading} onFile={f=>void onFile(f)}/><div className="space-y-1"><FieldLabel>Nombre</FieldLabel><TextField value={name} onChange={e=>setName(e.target.value)} placeholder="Nombre en el menú" required/></div><div className="space-y-1"><FieldLabel>Descripción</FieldLabel><TextArea className="min-h-20" value={description} onChange={e=>setDescription(e.target.value)} placeholder="Qué lleva, cómo se come…"/></div><div className="grid grid-cols-2 gap-2"><div className="space-y-1"><FieldLabel>Categoría</FieldLabel><select className="h-11 w-full rounded-[var(--radius-md)] bg-bg px-3 text-sm shadow-[var(--shadow-border)]" value={categoryId} onChange={e=>setCategoryId(e.target.value)}><option value="empanadas">Empanadas</option><option value="fajitas">Fajitas</option><option value="papas">Papas</option><option value="bebidas">Bebidas</option></select></div><div className="space-y-1"><FieldLabel>Precio (CLP)</FieldLabel><TextField inputMode="numeric" value={price} onChange={e=>setPrice(e.target.value.replace(/\D/g,""))}/></div></div><div className="flex gap-2"><Button type="submit" disabled={pending||uploading}>Guardar</Button><Button type="button" variant="ghost" onClick={()=>setOpen(false)}>Cancelar</Button></div></form>}
function ProductEditor({product,onPatch,onDelete,pending}:{product:Product;onPatch:(patch:Record<string,unknown>)=>void;onDelete:()=>void;pending:boolean}){const[name,setName]=useState(product.name),[description,setDescription]=useState(product.description),[price,setPrice]=useState(String(product.price)),[uploading,setUploading]=useState(false);const dirty=name.trim()!==product.name||description!==product.description||(Number(price)||0)!==product.price;async function onFile(file?:File){if(!file)return;setUploading(true);try{onPatch({imageUrl:await compressImage(file)})}catch(error){toast.error(error instanceof Error?error.message:"No se pudo subir")}finally{setUploading(false)}}return <article className="rounded-[var(--radius-lg)] bg-surface p-3 shadow-[var(--shadow-border)]"><div className="flex gap-3"><PhotoPicker imageUrl={product.imageUrl} uploading={uploading} onFile={f=>void onFile(f)}/><div className="min-w-0 flex-1 space-y-2"><div className="space-y-1"><FieldLabel>Nombre</FieldLabel><TextField value={name} onChange={e=>setName(e.target.value)}/></div><div className="flex items-end gap-2"><div className="min-w-0 flex-1 space-y-1"><FieldLabel>Precio (CLP)</FieldLabel><TextField inputMode="numeric" value={price} onChange={e=>setPrice(e.target.value.replace(/\D/g,""))}/></div><span className="mb-2.5 shrink-0 text-xs font-semibold text-heart">{formatClp(Number(price)||0)}</span></div><div className="flex items-center justify-end gap-2 pt-1"><span className="text-xs text-muted">{product.available?"Visible en el menú":"Oculto"}</span><Switch checked={product.available} onCheckedChange={v=>onPatch({available:v})}/></div></div></div><div className="mt-3 space-y-1"><FieldLabel>Descripción</FieldLabel><TextArea className="min-h-20" value={description} onChange={e=>setDescription(e.target.value)}/></div><div className="mt-3 flex items-center justify-between"><Button type="button" variant="ghost" size="sm" onClick={onDelete} className="text-heart"><Trash2 className="size-4"/>Quitar</Button><Button type="button" size="sm" disabled={!dirty||pending} onClick={()=>onPatch({name:name.trim()||product.name,description,price:Number(price)||0})}>Guardar cambios</Button></div></article>}
function ProductsTab({catalog}:{catalog:Catalog}){const queryClient=useQueryClient();const[filter,setFilter]=useState("empanadas");const list=catalog.products.filter(p=>filter==="all"||p.categoryId===filter);const patch=useMutation({mutationFn:(data:Record<string,unknown>)=>adminPatchProduct({data}),onSuccess:next=>{queryClient.setQueryData(["catalog"],next);toast.success("Guardado")},onError:(e:Error)=>toast.error(e.message)});const create=useMutation({mutationFn:(data:Record<string,unknown>)=>adminCreateProduct({data}),onSuccess:next=>{queryClient.setQueryData(["catalog"],next);toast.success("Producto creado")},onError:(e:Error)=>toast.error(e.message)});const remove=useMutation({mutationFn:(id:number)=>adminDeleteProduct({data:{id}}),onSuccess:next=>queryClient.setQueryData(["catalog"],next),onError:(e:Error)=>toast.error(e.message)});return <div className="space-y-4"><div className="flex gap-2 overflow-x-auto">{["all",...catalog.categories.map(c=>c.id)].map(id=><button key={id} type="button" onClick={()=>setFilter(id)} className={cn("h-9 shrink-0 rounded-full px-3 text-sm font-semibold",filter===id?"bg-primary text-primary-foreground":"bg-surface-2")}>{id==="all"?"Todos":catalog.categories.find(c=>c.id===id)?.name}</button>)}</div><NewProduct onCreate={data=>create.mutate(data)} pending={create.isPending}/>{list.map(product=><ProductEditor key={product.id} product={product} onPatch={p=>patch.mutate({id:product.id,...p})} onDelete={()=>remove.mutate(product.id)} pending={patch.isPending}/>)}</div>}
function IngredientsTab({catalog}:{catalog:Catalog}){const queryClient=useQueryClient();const mutation=useMutation({mutationFn:(data:Record<string,unknown>)=>adminPatchIngredient({data}),onSuccess:next=>queryClient.setQueryData(["catalog"],next),onError:(e:Error)=>toast.error(e.message)});return <div className="space-y-2"><p className="text-sm text-muted">El extra aplica a Arma tu fajita. En empanadas, mechada y llanero suman el extra premium.</p>{catalog.ingredients.map(ing=><div key={ing.id} className="flex flex-wrap items-center gap-3 rounded-[var(--radius-md)] bg-surface p-3 shadow-[var(--shadow-border)]"><div className="min-w-32 flex-1"><p className="font-semibold">{ing.name}</p><p className="text-xs text-muted">{ing.empanadaOk?"Empanada":""}{ing.empanadaOk&&ing.fajitaOk?" · ":""}{ing.fajitaOk?"Fajita":""}{ing.premium?" · premium":""}</p></div><div className="space-y-1"><FieldLabel className="text-xs">Extra fajita</FieldLabel><TextField className="w-24" inputMode="numeric" defaultValue={String(ing.fajitaPrice)} onBlur={e=>{const next=Number(e.target.value.replace(/\D/g,""))||0;if(next!==ing.fajitaPrice)mutation.mutate({id:ing.id,fajitaPrice:next})}}/></div><Switch checked={ing.available} onCheckedChange={v=>mutation.mutate({id:ing.id,available:v})}/></div>)}</div>}
function SettingsTab({catalog}:{catalog:Catalog}){const queryClient=useQueryClient();const settings=catalog.settings;const[form,setForm]=useState({restaurantName:settings.restaurantName,tagline:settings.tagline,city:settings.city,coverage:settings.coverage,deliveryNote:settings.deliveryNote,hours:settings.hours,prepMin:String(settings.prepMin),prepMax:String(settings.prepMax),whatsapp:settings.whatsapp,transferBank:settings.transferBank,transferName:settings.transferName,transferRut:settings.transferRut,transferAccount:settings.transferAccount,empanada1:String(settings.empanada1),empanada2:String(settings.empanada2),empanada3:String(settings.empanada3),empanadaPremium:String(settings.empanadaPremium),fajitaBase:String(settings.fajitaBase)});const[pin,setPin]=useState({current:"",next:""});const save=useMutation({mutationFn:()=>adminSaveSettings({data:{restaurantName:form.restaurantName,tagline:form.tagline,city:form.coverage,coverage:form.coverage,deliveryNote:form.deliveryNote,hours:form.hours,prepMin:Number(form.prepMin)||25,prepMax:Number(form.prepMax)||35,whatsapp:form.whatsapp,transferBank:form.transferBank,transferName:form.transferName,transferRut:form.transferRut,transferAccount:form.transferAccount,empanada1:Number(form.empanada1)||0,empanada2:Number(form.empanada2)||0,empanada3:Number(form.empanada3)||0,empanadaPremium:Number(form.empanadaPremium)||0,fajitaBase:Number(form.fajitaBase)||0}}),onSuccess:next=>{queryClient.setQueryData(["catalog"],next);toast.success("Ajustes guardados")},onError:(e:Error)=>toast.error(e.message)});const changePin=useMutation({mutationFn:()=>adminChangePin({data:pin}),onSuccess:async()=>{toast.success("PIN actualizado");setPin({current:"",next:""});await queryClient.invalidateQueries({queryKey:["admin-me"]})},onError:(e:Error)=>toast.error(e.message)});const fields=useMemo(()=>[["whatsapp","WhatsApp de la cocina (569…)"],["restaurantName","Nombre"],["tagline","Frase"],["coverage","Cobertura"],["hours","Horario"],["deliveryNote","Nota de delivery (sin inventar costo)"],["transferBank","Banco"],["transferName","Titular"],["transferRut","RUT"],["transferAccount","Cuenta"]] as const,[]);return <div className="space-y-6"><form className="space-y-3" onSubmit={e=>{e.preventDefault();save.mutate()}}>{fields.map(([key,label])=><div key={key} className="space-y-1"><FieldLabel>{label}</FieldLabel>{key==="whatsapp"?<p className="text-xs text-muted">Con código de país, sin +. Ej: 56912345678. Ahí llega el pedido completo.</p>:null}<TextField value={form[key]} onChange={e=>setForm({...form,[key]:e.target.value})}/></div>)}<p className="pt-2 text-sm font-semibold">Preparación</p><div className="grid grid-cols-2 gap-2"><div className="space-y-1"><FieldLabel>Mínimo (min)</FieldLabel><TextField inputMode="numeric" value={form.prepMin} onChange={e=>setForm({...form,prepMin:e.target.value.replace(/\D/g,"")})}/></div><div className="space-y-1"><FieldLabel>Máximo (min)</FieldLabel><TextField inputMode="numeric" value={form.prepMax} onChange={e=>setForm({...form,prepMax:e.target.value.replace(/\D/g,"")})}/></div></div><p className="pt-2 text-sm font-semibold">Arma tu empanada</p><div className="grid grid-cols-2 gap-2">{([["empanada1","1 ingrediente"],["empanada2","2 ingredientes"],["empanada3","3 ingredientes"],["empanadaPremium","Extra premium"],["fajitaBase","Base fajita"]] as const).map(([key,label])=><div key={key} className="space-y-1"><FieldLabel>{label}</FieldLabel><TextField inputMode="numeric" value={form[key]} onChange={e=>setForm({...form,[key]:e.target.value.replace(/\D/g,"")})}/></div>)}</div><Button type="submit" disabled={save.isPending}>Guardar ajustes</Button></form><form className="space-y-3 rounded-[var(--radius-lg)] bg-surface p-4 shadow-[var(--shadow-border)]" onSubmit={e=>{e.preventDefault();changePin.mutate()}}><p className="font-semibold">Cambiar PIN</p><TextField type="password" placeholder="PIN actual" value={pin.current} onChange={e=>setPin({...pin,current:e.target.value})}/><TextField type="password" placeholder="PIN nuevo" value={pin.next} onChange={e=>setPin({...pin,next:e.target.value})}/><Button type="submit" variant="secondary" disabled={changePin.isPending}>Actualizar PIN</Button></form></div>}
function Kitchen({initial}:{initial:Catalog}){const queryClient=useQueryClient();const catalog=useQuery({queryKey:["catalog"],queryFn:()=>getCatalog(),initialData:initial}).data!;const[tab,setTab]=useState("productos");const logout=useMutation({mutationFn:()=>adminLogout(),onSuccess:async()=>{await queryClient.invalidateQueries({queryKey:["admin-me"]})}});return <div className="mx-auto min-h-dvh max-w-3xl px-4 py-5"><div className="flex items-center justify-between gap-3"><div className="flex items-center gap-2"><Button variant="ghost" size="icon" asChild aria-label="Menú"><Link to="/"><ArrowLeft className="size-5"/></Link></Button><BrandLockup compact/></div><Button variant="secondary" size="sm" onClick={()=>logout.mutate()}><LogOut className="size-4"/>Salir</Button></div><h1 className="mt-6 font-display text-3xl font-semibold">Cocina</h1><p className="mt-1 text-sm text-muted">Sube fotos, escribe la descripción y cambia el precio. Se ve al tiro en el menú.</p>{normalizeWhatsapp(catalog.settings.whatsapp).length>=11?null:<p className="mt-3 rounded-[var(--radius-md)] bg-surface-2 px-3 py-2 text-sm">Falta el WhatsApp de la cocina en Ajustes. Sin ese número el cliente igual abre WhatsApp con el pedido completo.</p>}<div className="mt-6"><div className="flex w-full gap-1 overflow-x-auto rounded-full bg-surface-2 p-1 shadow-[var(--shadow-border)]">{[["productos","Productos"],["combos","Combos"],["ingredientes","Ingredientes"],["ajustes","Ajustes"]].map(([id,label])=><button key={id} type="button" onClick={()=>setTab(id)} className={cn("shrink-0 rounded-full px-4 py-2 text-sm font-semibold text-muted transition-colors",tab===id&&"bg-surface text-fg shadow-[var(--shadow-border)]")}>{label}</button>)}</div><div className="mt-4">{tab==="productos"?<ProductsTab catalog={catalog}/>:null}{tab==="combos"?<ComboAdmin/>:null}{tab==="ingredientes"?<IngredientsTab catalog={catalog}/>:null}{tab==="ajustes"?<SettingsTab catalog={catalog}/>:null}</div></div></div>}
export function AdminPage({initial}:{initial:Catalog}){const me=useQuery({queryKey:["admin-me"],queryFn:()=>adminMe()});if(me.isLoading)return <div className="flex min-h-dvh items-center justify-center text-muted">Cargando panel…</div>;if(me.data?.ok)return <Kitchen initial={initial}/>;return <Login/>;}
